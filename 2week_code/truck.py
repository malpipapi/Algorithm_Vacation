def solution(bridge_length, weight, truck_weights):
    answer = 0
    success_truck=[]
    ing_truck=[]
    truck_end=len(truck_weights)
    bridge_len=0
    timer=0
    i=0
    current_weight=0
    current_length=0
    while len(truck_end)!=len(success_truck):

    return answer

bridge_length=2
weight=10
truck_weights=[7,4,5,6]
print(solution(bridge_length, weight, truck_weights))
lena=[]
print(sum(lena))